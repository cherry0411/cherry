# Copy to .env for the local Docker Compose stack. Never commit the real file.
POSTGRES_DB=cherry
POSTGRES_USER=cherry
POSTGRES_PASSWORD=replace-with-a-random-local-password
CHERRY_API_KEY=replace-with-a-random-local-api-key
